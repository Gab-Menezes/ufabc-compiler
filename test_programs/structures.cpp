#include <iostream>
#include <string>

int main() {
    long long int a = (1);
    double b = (1.0);
    bool c = (true);
    std::string d = std::string("abc");
    if ((c)) {
        for (; ((a) < (10)); a += (2)) {
            std::cout << d << '\n';
        }
    }
    if (((d) == std::string("abc"))) {
        while (((b) <= (10.0))) {
            std::cin >> a;

        }
    }

    return 0;
}